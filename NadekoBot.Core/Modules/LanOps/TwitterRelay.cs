using Discord.Commands;
using NadekoBot.Core.Services;
using NadekoBot.Modules;
using System;
using System.Collections.Generic;
using System.Text;
using Tweetinvi;
using Tweetinvi.Models;

namespace NadekoBot.Core.Modules.LanOps
{
  /*  [Group]
    public class TwitterRelay : NadekoTopLevelModule, INService
    {


        public TwitterRelay(DiscordSocketClient c)
        {
            _client = c;
            LoadConfig();
            lock (watchedLans)
            {
                if (!running)
                {
                    running = true;
                    System.Threading.Tasks.Task.Run(WatchForNewAttendees);
                }
            }
        }

        private async System.Threading.Tasks.Task WatchForNewAttendees()
        {
            var creds = new TwitterCredentials(
            Stream.CreateTweetStream(new Twitt)

            Dictionary<string, List<Participant>> lastCheck = new Dictionary<string, List<Participant>>();
            while (true)
            {
                try
                {
                    


                }
                catch (Exception e)
                {
                    Console.WriteLine(DateTime.Now.ToString("d") + "Failed checking for attendees.." + e.Message);
                }

                await System.Threading.Tasks.Task.Delay(1000 * 60 * 30);

                //  await Task.Delay(1000 * 60 * 30);
            }
        }

    }*/
}